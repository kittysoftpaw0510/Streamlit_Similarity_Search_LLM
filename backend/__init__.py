"""
Package marker for backend module. Adding this file makes `backend` a proper
Python package so imports like `backend.main` work when running Uvicorn as a module.

Do not add side-effectful initialization here.
"""
